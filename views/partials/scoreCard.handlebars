<section class="cards">
<div class="card" data-score="{{score}}" data-name="{{name}}">
  <div class="card-contents">
    <div class="post-info-container">
      <a href="#" class="card-title">{{name}}</a> <span class="card-score">${{score}}</span>
    </div>
  </div>
</div>
</section>
